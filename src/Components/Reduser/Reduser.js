const initialState = {
    counter: 0,
    name: "btn Zero",
    btnPlus:"Plus"
};

const reducer = (state = initialState,action) =>{
    switch (action.type) {
        case "INC":
            
            break;
    
        default:
            break;
    }
}